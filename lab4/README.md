# 4я лабораторная, методы оптимизации
## f(x) = 2*x1-5*x2+exp((x1^2) + (0.5*(x2)^2))
    1. решить задачу методом первого порядка для скорейшего спуска. 
    произвести вычисления с точностью 0,1 0,01 0,01
    2. показать ортогональность звеньев по градиентной ломанной для одного значения точности
    3. построить геометрическую иллюстрацию с уровнями градиента 
    4. решить ту же задачу методом хука джинса 
    5. решить ту же задачу методом БФГД второго порядка 
    6. обоснование применимости сделать на основе любой теоремы на выбор(сходимости или скорости сходимости)
